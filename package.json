{
  "name": "api-base",
  "version": "1.0.0",
  "description": "an api for studies",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' server.ts && yarn migrate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "yarn knex migrate:latest && yarn knex seed:run"
  },
  "author": "cesar soares",
  "license": "ISC",
  "dependencies": {
    "@types/knex": "^0.14.18",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.18.3",
    "compression": "^1.7.3",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.3.0",
    "knex": "^0.15.2",
    "objection": "^1.2.2",
    "pg": "^7.4.3",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.5.2",
    "typescript": "^2.9.2"
  }
}
